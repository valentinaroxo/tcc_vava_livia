extends layout

block content

  h1 #{title}

  form(method='POST')
    div.form-group
      label(for='name') Modalidade:
      input#name.form-control(type='text', placeholder='Atletismo, Basquete, Futsal etc.' name='name' required value=(undefined===genre ? '' : genre.name) )
    button.btn.btn-primary(type='submit') Salvar

  if errors
    ul
      for error in errors
        li!= error.msg