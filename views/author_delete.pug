extends layout

block content

  h1 #{title}: #{author.name}
  p= author.lifespan

  if author_books.length

    p #[strong Exclua os seguintes esportes antes de tentar excluir este aluno.]
    div(style='margin-left:20px;margin-top:20px')
      h4 Esportes
      dl
        each book in author_books
          dt
            a(href=book.url) #{book.title}
          dd #{book.summary}

  else
    p Deseja realmente excluir este aluno?

    form(method='POST')
      div.form-group
        input#authorid.form-control(type='hidden', name='authorid', value=author._id )

      button.btn.btn-primary(type='submit') Excluir